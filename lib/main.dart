import 'package:aplikasi_ig/buttomNavigasu.dart';
import 'package:flutter/material.dart';

void main() => runApp(AplikasiIG());

class AplikasiIG extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MyBottomNavigationBar(),
    );
  }
}
